# CSC Mitra AI — Streamlit Cloud requirements
# Optimised for fast cold-start on Streamlit Community Cloud.
# No chromadb / sentence-transformers / HuggingFace model downloads.
# Vector store: pgvector (Neon/Supabase) in production; offline builtin guides as fallback.

# ── Streamlit ─────────────────────────────────────────────────────────────
streamlit>=1.32.0
streamlit-mic-recorder

# ── LLM access ───────────────────────────────────────────────────────────
openai>=1.0.0

# ── Workflow orchestration ────────────────────────────────────────────────
langgraph>=0.0.10

# ── Database (grievance service) ─────────────────────────────────────────
sqlalchemy>=2.0.23
psycopg2-binary>=2.9.9      # pgvector KB + Postgres grievance DB
                             # (SQLite fallback requires no extra package)

# ── Data validation ───────────────────────────────────────────────────────
pydantic>=2.4.2
python-dotenv>=1.0.0

# ── Web / crawling ────────────────────────────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0

# ── Document ingestion ────────────────────────────────────────────────────
pypdf>=3.0.0
python-docx>=1.0.0
openpyxl>=3.1.0
python-pptx>=0.6.23

# ── Token counting (optional, graceful fallback if missing) ───────────────
tiktoken>=0.5.0
